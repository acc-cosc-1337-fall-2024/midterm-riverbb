// In the header file, write a string value return function prototype get_factorial_sequence with an int parameter.

#include <string>

bool test_config();

std::string get_factorial_sequence(int number);
